import {
  getAllBooks,
  getSingleBookById,
  getSingleBookByUniquekey,
  searchQueriesBooks,
  getAllBookscategories,
  getSingleBookCategory,
} from "./book.get.js";

const books = {
  getAllBooks,
  getSingleBookById,
  getSingleBookByUniquekey,
  searchQueriesBooks,
  getAllBookscategories,
  getSingleBookCategory,
};

export default books;
